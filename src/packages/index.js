import Button from './button'
import Select from './select'
import Rate from './rate'

const components = {
  'hm-button': Button,
  'hm-select': Select,
  'hm-rate': Rate,
}

export default components
